# kitu-app-actions

Shared runtime action definitions for Kitu.

This crate owns the action catalog model used by the logic runtime, web admin,
and `kitu-cli`. It provides fixed Kitu general actions plus manifest loading for
project-specific actions.
